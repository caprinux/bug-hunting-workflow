{
  "type": "object",
  "properties": {
    "narrative": {
      "type": "string",
      "description": "Brief progress note (1-2 sentences) for the human-readable conversation log. Always populate. Plain text, no markdown. Empty string only if you have nothing to say."
    },
    "validated": {
      "type": "boolean"
    },
    "verdict": {
      "type": "string",
      "enum": [
        "confirmed",
        "legitimate",
        "not_real"
      ]
    },
    "poc": {
      "type": "object",
      "properties": {
        "language": {
          "type": "string"
        },
        "code": {
          "type": "string"
        },
        "execution_result": {
          "type": "string",
          "enum": [
            "success",
            "failure",
            "error",
            "destructive_skipped"
          ]
        },
        "output": {
          "type": "string"
        }
      },
      "required": [
        "language",
        "code",
        "execution_result",
        "output"
      ],
      "additionalProperties": false
    },
    "reason": {
      "type": "string"
    }
  },
  "required": [
    "narrative",
    "validated",
    "verdict",
    "poc",
    "reason"
  ],
  "additionalProperties": false
}