fastapi>=0.115.0
uvicorn[standard]>=0.32.0
websockets>=13.0
pyyaml>=6.0
python-multipart>=0.0.9
aiofiles>=24.0
httpx>=0.27.0

# Agent SDKs
claude-agent-sdk>=0.2.94            # required — drives the Claude hunting/validation agents
openai-codex>=0.1.0b3               # optional — enables the Codex (GPT) agent (official SDK, currently beta; the prerelease pin lets pip install it without a global --pre)
